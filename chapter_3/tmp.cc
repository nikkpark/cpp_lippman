#include <iostream>
#include <string>
#include <cstring>
using namespace std;

int main() 
{
    //string s("some string");
    //if (s.begin() != s.end()) {
        //auto it = s.begin();
        //*it = toupper(*it);
    //}
    //cout << s << endl;
    //string p("some string");
    //for (auto it2 = p.begin(); it2 != p.end() && !isspace(*it2); ++it2)
        //*it2 = toupper(*it2);
    //cout << p << endl;
    //unsigned scores[11] = {};
    //unsigned grade;
    //while (cin >> grade) {
      //if (grade <= 100)
        //++scores[grade/10];
      //}
    
    //for (auto &i : scores)
      //cout << i << " ";
    char ca[] = {'c', '\0'};
    cout << strlen(ca);
    return 0;
}

